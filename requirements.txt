# Generated from pyproject.toml -- Render builds with pip, not uv.
# Regenerate after changing pyproject: see DEPLOY.md.
# torch is the CPU wheel; PyPI serves CPU-only torch on Linux x86_64 by default
# for this version, which is what we want (no GPU on Render).
asyncpg==0.31.0
chainlit==2.11.1
fastapi==0.141.1
httpx==0.28.1
huggingface-hub==1.26.0
numpy==2.5.1
psycopg[binary]==3.3.4
python-dotenv==1.2.2
rank-bm25==0.2.2
requests==2.34.2
selectolax==0.4.11
sqlalchemy==2.0.51
torch==2.13.0
transformers==5.14.1
uvicorn==0.52.1
